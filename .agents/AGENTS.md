All design decisions and agent rules for this repo live in [DESIGN.md](./DESIGN.md). Read it before touching any UI, component, copy, or commit.

This file is scoped to `.agents/` on purpose: the root `AGENTS.md` is owned and regenerated by `next dev` (Next.js version-specific rules) and must not be repurposed to hold project rules.
